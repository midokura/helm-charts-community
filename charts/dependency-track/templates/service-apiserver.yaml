apiVersion: v1
kind: Service
metadata:
  name: {{ include "dependency-track.fullname" . }}-apiserver
  labels:
    {{- include "dependency-track.api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dependency-track.api.selectorLabels" . | nindent 4 }}
