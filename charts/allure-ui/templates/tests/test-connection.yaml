apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "allure-ui.fullname" . }}-test-connection"
  labels:
    {{- include "allure-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "allure-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
