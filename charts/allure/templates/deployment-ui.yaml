apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "allure.fullname" . }}-ui
  labels:
    {{- include "allure.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "allure-ui.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "allure.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}-ui
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.imageUI.repository }}:{{ .Values.imageUI.tag }}"
          imagePullPolicy: {{ .Values.imageUI.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "allure.fullname" . }}-ui
          ports:
            - name: ui
              containerPort: 5252
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /allure-docker-service-ui/version
              port: ui
          readinessProbe:
            httpGet:
              path: /allure-docker-service-ui/version
              port: ui
